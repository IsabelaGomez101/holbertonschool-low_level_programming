#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
char c;
c = a;
while(a <= z)
{
putchar(c);
c++;
}
putchar('\n');
return (0);
}
